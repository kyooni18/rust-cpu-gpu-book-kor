# 집필 스타일 가이드

이 책의 모든 장은 아래 규칙을 따릅니다. 리뷰 역시 이 기준으로 진행합니다.

## 문체

- 존댓말 문체로 통일합니다
- 한 문장은 짧게 씁니다. 쉼표로 지나치게 많은 절을 연결하지 않습니다
- 불필요하게 긴 도입부나 같은 표현의 반복을 피합니다
- 과장된 표현을 사용하지 않습니다. 성능 차이는 가능한 한 실제 수치로 보여 줍니다

## 용어

- 전문 용어는 처음 등장할 때 반드시 설명합니다. 첫 등장은 `<span class="term">용어</span>`가 아니라 **굵은 글씨** + 영어 병기로 표시합니다. 예: **캐시 라인**(cache line)
- 처음 정의한 전문 용어는 부록의 용어집(`appendix/glossary.mdx`)에도 추가합니다
- 불필요한 외래어와 전문 용어 남용을 피합니다. 널리 정착된 한국어 번역어가 있다면 이를 우선합니다
- 한국어로 옮겼을 때 오히려 어색한 용어는 캐시, 파이프라인처럼 자연스러운 음역을 사용해도 됩니다

## 비유

- 비유는 원칙적으로 최소화합니다
- 꼭 도움이 될 때만 단순한 직유를 사용할 수 있습니다
- CPU나 컴파일러를 사람처럼 의인화하지 않습니다

## 수치

- 지연 시간과 용량 같은 값이 근사값이라면 "대략", "약", "~정도"처럼 근사치임을 명시합니다
- 실제 측정값을 제시할 때는 가능한 한 측정 환경을 함께 적습니다
- 근거 없이 단정적인 배율을 적지 않습니다. 성능 차이는 실측으로 제시합니다

## 코드

- 브라우저에서 실행할 수 있는 코드는 `src/snippets/chNN/*.rs`에 두고 `?raw`로 가져와 `<RustPlay>`에 전달합니다. 화면 표시, 실행, 컴파일 검증이 같은 파일을 사용하도록 합니다
- 직접 실행할 필요가 없는 짧은 코드 조각은 일반 코드 펜스를 사용합니다
- 모든 스니펫은 `rustc --edition 2024`로 컴파일되어야 합니다
- nightly 기능이 필요한 스니펫은 `src/snippets/chNN/nightly-*.rs` 이름을 사용하고 `<RustPlay channel="nightly">`로 표시합니다
- 성능 측정 스니펫은 Rust Playground의 실행 시간 제한 안에 들어오도록 크기를 조절합니다
- 코드 안의 설명 주석은 한국어로 작성하고 핵심 내용만 남깁니다

## 장 구성

1. 시작: "이 장에서 알 수 있는 것" 목록을 3~5개 정도 둡니다
2. 본문: 제목은 h2와 h3만 사용하며 하나의 h2는 하나의 주제를 다룹니다
3. 각 장에는 최소 하나의 실험 또는 실행 가능한 코드가 있어야 합니다. GPU처럼 브라우저 실행이 어려운 장은 로컬 실행 절차로 대신할 수 있습니다
4. 끝: "정리" 목록과 다음 장으로 이어지는 1~2문장을 둡니다

## 그림

- 개념의 흐름과 구조는 가능하면 Mermaid 코드 블록으로 작성합니다
- Mermaid로 표현하기 어려운 그림은 인라인 SVG를 사용하고 `.book-figure`로 감싸며 `figcaption`을 추가합니다
- 그림은 본문에서 반드시 설명합니다. 그림만 두고 설명하지 않는 구성을 피합니다

## 링크

- 장 사이 링크는 `/cpu/01-how-code-runs/` 같은 절대 경로를 사용합니다
- 외부 링크는 공식 문서와 1차 자료를 우선합니다

## 추가 규칙

- "이 장에서 알 수 있는 것" 목록은 장의 예고입니다. 전문 용어의 정식 첫 정의는 본문에서 **굵은 글씨** + 영어 병기로 작성합니다

## 응용편(ch13~28) 추가 규칙

- 관련된 기초편 장으로 돌아가는 링크를 반드시 넣어 전체 체계가 이어지도록 합니다
- 각 장의 "이 장에서 알 수 있는 것" 마지막 항목에는 왜 전체 체계에 필요한지 설명합니다
- 실험은 가능한 한 Playground에서 실행할 수 있도록 합니다. GPU나 OS 도구처럼 어렵다면 `examples/`와 로컬 측정으로 대체합니다
- 직접 실험하기 어려운 주제는 관찰 절차나 측정 방법을 제시하는 방식으로 대신합니다
- 일반적인 정석이 실제 측정에서 뒤집혔다면 숨기지 않고 결과와 이유를 그대로 설명합니다
